# SQL — Data Quality Checks

Quality checks are database-side controls.

## Controls

- source row count
- distinct ABN count
- ABN validity
- NULL / non-NULL counts by column
- duplicate detection
- referential integrity
- financial accounting rules
- outlier review flags
- ingestion acceptance status

The authoritative output is `analytics.acnc_data_quality_report` and its summary view.
